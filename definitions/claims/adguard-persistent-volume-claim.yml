apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: network
  name: adguard-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: microk8s-hostpath
