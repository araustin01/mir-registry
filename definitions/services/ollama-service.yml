apiVersion: v1
kind: Service
metadata:
  namespace: alice
  name: ollama-service
spec:
  type: NodePort
  selector:
    app: ollama
  ports:
    - protocol: TCP
      port: 11434
      targetPort: 11434
      nodePort: 30434  # Choose a port within the range 30000-32767
