apiVersion: v1
kind: Service
metadata:
  namespace: web-services
  name: pds-service
  labels:
    app: bluesky-pds
spec:
  type: LoadBalancer
  selector:
    app: pds
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
